# Minimal pip packages required to reproduce Tier-A plumbing, E1 scoring, and verification.
# Use Python 3.11.9; Python itself is not a pip dependency.
# The workspace env.lock also contains unrelated packages (TensorFlow, OpenCV,
# and an editable VCS install of ask-the-judge-twice). Do not treat those as
# paper dependencies. Offline Hugging Face snapshots and models/lid.176.ftz are
# additional local prerequisites and are not installed by pip.

torch==2.6.0+cu124
transformers==5.14.1
accelerate==1.14.0
peft==0.20.0
datasets==5.0.1
numpy==2.4.2
scipy==1.17.1
safetensors==0.8.0
fasttext-wheel==0.9.2
